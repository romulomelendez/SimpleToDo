import styled from "styled-components"

export const AgendaContainer = styled.section `

    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    border-radius: 0.5rem 0.5rem 0 0;
    row-gap: 1rem;
    height: 6rem;
    width: 100%;
    background: rgb(44,243,247);
background: linear-gradient(90deg, rgba(44,243,247,1) 0%, rgba(78,192,254,1) 50%);

`

export const AgendaTitle = styled.h1 ` color: var(--white); `

export const AgendaInput = styled.input ``
