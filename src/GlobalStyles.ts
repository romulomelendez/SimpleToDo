import { createGlobalStyle } from 'styled-components'

export const GlobalStyle = createGlobalStyle`
  
  :root {
    --white: white;
    --light-blue: #2CF3F7;
    --background-color-primary: #E3F7FE;
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

`